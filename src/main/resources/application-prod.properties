# Producción / contenedor Coolify: misma red Docker que PostgreSQL.
# No uses aquí la IP/puerto públicos (5433); usa el host interno que muestra Coolify en Credentials.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable&ApplicationName=LuciaAPI}
