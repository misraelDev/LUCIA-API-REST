-- Menú sugerido para rol admin (incluye la nueva pantalla de tenants).
-- Ajusta id del tenant y ejecuta UPDATE.

-- UPDATE public.tenants
-- SET navigation_config = '{
--   "admin": ["admin_home", "admin_users", "admin_tenants"],
--   "user": ["user_home"],
--   "seller": ["user_home"]
-- }'
-- WHERE id = 1;
